#!/usr/bin/python3

def number_keys(a_dictionary):
    """
    Returns the number of keys in a dictionary.
    parameters:
    a_dictionary: key, value pairs
    return
    int: number of keys
    """
    return len(a_dictionary.keys())
